export interface UserInfo {
  id: string;
  email: string;
  minecraft_id: string;
  username: string;
  zodiac: string;
  village: number;
  admin: boolean;
  is_approved: boolean;
  created_at: string;
}
